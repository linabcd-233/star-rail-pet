# 银枝桌宠 — 技术文档

> **路径**：本文件位于 **`docs/技术文档.md`**（早期曾在仓库根目录，请以当前路径为准）。  
> **进度**：**桌宠主程序**已落在 **`pet-web/`**（Tauri + Vite + 官方 Spine WebGL）；**Python 自研预览线已搁置维护**（不接新功能）。下文「分层/选型」仍可作为背景，**运行命令与目录以 [`README.md`](../README.md)、[`pet-web/README.md`](../pet-web/README.md) 为准**。

## 1. 可行性结论

| 方向 | 结论 |
|------|------|
| **桌宠形态** | 可行。Windows 上常用：无边框置顶窗口 + 透明/分层（PyQt6、Electron、或 pygame + `pygame.Window`/`SRCALPHA` 等方案）。 |
| **骨骼动画** | **数据已具备**：`1302.1a88ff13_ske.json` 为 **DragonBones 5.5** 骨架与动画列表；同目录 `1302.1a88ff13.json` 为 **Spine 4.2** 导出。二选一接入官方或社区运行时即可，不必从零解析曲线与网格。 |
| **贴图** | `1302.atlas` 引用 **`1302.png`**。若目录中缺少 PNG，线框预览仍可运行；**完整角色显示需补全图集 PNG**（与 atlas 同目录）。 |
| **交互（点击/拖拽/跟随）** | 可行，属于窗口级鼠标事件 + 状态机。 |
| **养成（随机词条）** | 纯逻辑，用 `random` 即可，与渲染解耦。 |
| **番茄钟 / 待办** | 可行。当前仓库已在 **`pet-web`** 内实现 **番茄钟 MVP**（DOM 面板 + `emoji_5` 台词）；Python 线仍可用定时器/通知类库扩展。 |

**风险点**：若坚持 **纯 Python + Pygame 自研 DragonBones 播放器**，工作量极大（网格变形、IK、曲线插值、换肤）。**本仓库实际选型**：桌宠侧采用 **Tauri + WebView + Spine 官方 JS 运行时**（见 `pet-web/`）；Python 侧保留 **自研 Spine 网格预览** 作工具链，与桌宠 **并行**。

---

## 2. 技术架构（建议分层）

```
┌─────────────────────────────────────┐
│  UI 壳层：菜单、番茄钟、待办、设置   │
├─────────────────────────────────────┤
│  桌宠核心：窗口、拖拽、置顶、多显示器 │
├─────────────────────────────────────┤
│  角色表现：骨骼动画状态机、音效      │
├─────────────────────────────────────┤
│  动画运行时：Spine / DragonBones     │
├─────────────────────────────────────┤
│  资源：ske/json、atlas、png、音频    │
└─────────────────────────────────────┘
```

- **表现数据**：待机 / 行走 / 交互 等映射到动画名（当前 ske 中可见如 `idel`、`emoji_*` 等，具体以工程内动画名为准）。
- **养成系统**：独立模块，通过事件通知 UI（例如“刷出一条风套词条”弹条），不阻塞动画线程。

---

## 3. 文件架构（与当前仓库对齐）

```
star-rail-pet/
├── assets/argenti/           # 银枝 Spine 资源（主数据源）
│   ├── 1302.1a88ff13.json    # Spine 4.2：网格预览与 pet-web 共用
│   ├── 1302.1a88ff13_ske.json # DragonBones线框预览（`preview_skeleton.py`）
│   ├── 1302.atlas
│   └── 1302.png
├── src/star_rail_pet/        # Python 包：自研网格预览（已拆分模块）
├── pet-web/                  # Tauri 桌宠：官方运行时、番茄钟、穿透等
│   ├── scripts/sync-assets.mjs
│   └── public/argenti/       # 由 sync-assets 从 assets/argenti 复制
├── tools/preview_spine_character.py
├── preview_skeleton.py       # 根目录：DragonBones ske 线框（默认 assets/argenti）
├── preview_spine_character.py
├── requirements.txt
└── docs/
    ├── 技术文档.md           # 本文件
    └── 架构规划.md              # 更细的 Python 目录与迁移说明
```

**代码已拆分**：Python 模块说明见 **`docs/架构规划.md`**；前端模块见 **`docs/前端模块拆分与交互落地规划.md`**。

**Spine 贴图预览（默认资源 `assets/argenti/`）**：

```bash
pip install -e .
python tools/preview_spine_character.py --anim idel
# 或：python -m star_rail_pet --anim idel
# 兼容：python preview_spine_character.py --dir "其他目录"
```

---

## 4. 依赖与技术栈选型

### 4.1 最小验证（已实现）

| 依赖 | 用途 |
|------|------|
| **Python 3.10+** | 运行环境 |
| **pygame ≥ 2.5** | 窗口 + 骨骼**线框**绘制（不解析完整 DragonBones 动画曲线） |

安装：

```bash
pip install -r requirements.txt
```

### 4.2 正式桌宠（择一扩展）

| 方案 | 典型依赖/技术 | 说明 |
|------|----------------|------|
| **Tauri + Web + Spine 官方 JS**（**本仓库已采用**） | Rust、WebView2、`@esotericsoftware/spine-*` | 壳小、与 `pet-web/` 当前实现一致；资源经 `sync-assets` 对齐 `assets/argenti`。 |
| **PyQt6 / PySide6** | `PyQt6`, `QTimer`, 可选 `QMediaPlayer` | 商业友好需注意许可；适合做半透明置顶窗与系统托盘。 |
| **Electron + Web** | Node, `pixi-spine` 或 DragonBones JS | 动画生态好，包体较大。 |
| **Godot 4** | GDScript / C# | 2D 骨骼工作流顺，导出桌面工程。 |
| **Spine 官方运行时** | C/C++ 或绑定语言 | 非 Web 绑定时可选。 |

### 4.3 工具向功能

| 功能 | 可选依赖 |
|------|----------|
| 系统通知 | `plyer` 或 WinRT Toast |
| 配置持久化 | `pydantic` + JSON / `tomllib` |
| 打包 exe | `pyinstaller` / `cx_Freeze` |

---

## 5. 资源格式说明

- **`1302.1a88ff13_ske.json`**：`frameRate`、`armature`、`bone`、`slot`、`skin`、`animation` 等字段 → **DragonBones**。
- **`1302.1a88ff13.json`**：顶层含 `"spine":"4.2.40"` → **Spine** 骨骼 JSON；**带贴图网格预览与 `pet-web` 运行时均以本文件为主**。  
- **二者关系**：不要用 DB ske 与 Spine JSON **混在同一套 mesh 播放逻辑里**；线框用 ske，网格用 Spine JSON。
- **`1302.atlas` + `1302.png`**：Spine/DragonBones 共用风格的图集描述；**缺 PNG 则无法显示贴图**。

---

## 6. 预览脚本用法

在项目根目录：

```bash
# 骨骼线框（DragonBones ske；皮肤数据若损坏则只能看线框）
python preview_skeleton.py
python preview_skeleton.py "D:\path\to\1302.1a88ff13_ske.json"
python preview_skeleton.py --summary-only

# 带贴图的角色（默认仓库内 assets/argenti；也可用 --dir 指向其他目录）
pip install -r requirements.txt
python tools/preview_spine_character.py --anim idel

# 程序内循环播放骨骼动画（线性插值；不含 slot 附件切换 / IK / 网格变形 / 贝塞尔曲线）
python tools/preview_spine_character.py --anim emoji_2

# 仅看整张图集缩略图（纯 pygame，无需 OpenGL）
python tools/preview_spine_character.py --mode bitmap
```

说明：**DragonBones 的 `*_ske.json` 里皮肤常为占位，无法驱动贴图**；拼装预览依赖 **`1302.1a88ff13.json`（Spine 4.2）** 与 **`1302.png` 图集**。本仓库不按散图目录加载；修图或换皮应在 Spine 中重新打包 atlas 后再导出。

**若拼装失真、错层、色块、边缘糊：**  
- **UV**：JSON 里 mesh 的 `uvs` 是 Spine 的 *regionUVs*，必须结合 atlas 的 `offsets`（原始尺寸与裁剪偏移）与 `rotate` 换算；旧版脚本若直接用 `bounds` 映射会采到邻图，表现为错位与色块。当前 `preview_spine_character.py` 已按官方 `spMeshAttachment_updateRegion` 对齐。  
- **模糊**：`1302.atlas` 里常见 `scale:0.2`，表示图集按 **20% 分辨率** 导出，放大预览必然偏糊；需从工程 **导出更高分辨率** 图集（或去掉低倍率 scale）。  
- **混合**：若图集含 `pma:true`（预乘 Alpha），需用预乘混合；脚本会读 `pma:` 行。  
- **过滤**：默认与 atlas `filter:Linear` 一致；可加 `--nearest` 试最近邻（更硬、更像素，略减糊感但仍有锯齿）。

窗口内 **ESC** 或关闭窗口退出。

---

## 7. 里程碑（历史建议 ↔ 当前进度）

**早期建议顺序**（仍适用于仅 Python 线或新功能规划时对照）：

1. 将 `1302.png` 与 atlas、json 放入 `assets/argenti/`。  
2. 跑通 **Spine** 带贴图网格预览（DragonBones ske 仅作线框工具）。  
3. 接入 **idel** 等动画播放与状态切换。  
4. 无边框置顶窗与拖拽。  
5. 番茄钟、待办、随机词条等。

**截至当前仓库**（简要）：**2～4及番茄钟 MVP** 已在 **`pet-web`** 侧落地（透明窗、像素命中、拖窗、`SpineCanvas` 轮播可选、番茄钟与台词等）；**交互仲裁**（流汗 / 星星眼等）见 **`docs/交互与表情仲裁设计.md`**，部分仍为规划。Python 线仍以预览与工具为主。

---

*文档版本：路径 `docs/技术文档.md`；命令默认 `assets/argenti`与 `tools/` 入口以仓库为准。*
